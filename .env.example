# HuggingFace Token (Required for gated models like Ministral 3)
# Get one at: https://huggingface.co/settings/tokens
HF_TOKEN=hf_...

# MCP Configuration (Optional)
# Command to run a local MCP server (e.g., "uvx", "python", etc.)
MCP_SERVER_COMMAND=
# Arguments for the MCP server command (JSON list, e.g., '["mcp-server-sqlite", "--db", "test.db"]')
MCP_SERVER_ARGS=[]